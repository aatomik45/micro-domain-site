"""Authentication routes removed during standalone cleanup.

This project no longer ships with the previous OIDC-based auth flow.
If you want login later, add a new provider intentionally instead of
re-enabling the old platform-specific implementation.
"""
