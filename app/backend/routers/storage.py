"""Storage admin routes removed during standalone cleanup.

These endpoints depended on the removed auth system and were disabled to
avoid exposing object-storage management without a replacement security
layer.
"""
